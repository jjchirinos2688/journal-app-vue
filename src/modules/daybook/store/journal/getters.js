export const getEntryByTerm = (/* state */) => {

    console.log('Getter que obtiene las entradas buscadas por algún término');
}

export const getEntryById = (/* state */) => {

    console.log('Getter que obtiene una entrada buscada por ID');
}